#include <iostream>  
#include <cmath>
using namespace std;

void main()
{
    double x, y;
    cin >> x;
    cout << " p = " << pow(x, 5) + 5 * pow(x, 4) + 10 * pow(x, 2) + 5 * x + 1 << endl;
}